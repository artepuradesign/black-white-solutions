import PageShell from "@/components/layout/PageShell";

const Dashboard = () => {
  return (
    <PageShell>
      <div className="flex min-h-[calc(100vh-4rem)] items-center justify-center">
        <h1 className="text-5xl font-bold tracking-wide">Dashboard</h1>
      </div>
    </PageShell>
  );
};

export default Dashboard;
